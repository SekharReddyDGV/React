// 13.Check if both boolean values are true using logical AND (&&).

function logical(value1,value2){
 var compare= value1 && value2 ;
    return compare;
}
const check=logical(true,true);
console.log("both boolean values are:", check);